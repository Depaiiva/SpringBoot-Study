spring:
  application:
    name: User API
  datasource:
    url: jdbc:h2:mem:user
    username: sa
    password: password
  jpa:
    database-plataform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console